// Shared across admin/dashboard, admin/reports, and admin/inventory so the
// same catalog doesn't show three different "low stock" counts depending on
// which admin tab you're looking at.
export const LOW_STOCK_THRESHOLD = 5;
